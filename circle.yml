dependencies:
  cache_directories:
    - ~/.meteor
  override:
    - meteor || curl https://install.meteor.com | /bin/sh
    - meteor npm install

test:
  override:
    - eslint client/ imports/ server/